# V1 Known Limitations

This document records limitations that remain after the implemented Priority 1 engineering controls. It is evidence for the P10 known-limitations criterion; it does not waive any incomplete acceptance gate or unlock Priority 2.

## Release-blocking evidence gaps

### Current local Studio checkpoint — 12 September 2026

- Release `59ad760` and schema `0042_evaluation_references` are deployed to the same Desktop engine and source bind, with no migration drift. The actual database has **three originals / 376 pages: 370 failed readings and six needing review**, zero verified/excluded pages, human references, legacy ground truth, chunks and embeddings. One fixed nine-page review set and nine audited comparison previews are prepared; all reference fields remain blank. All 425 managed files and protected operational data were preserved. The [current reference-only queue](http://localhost:3000/admin/materials/benchmark-review?benchmark_id=9fd3f582-44c2-4f96-a8ed-55d3931e9f50) is a collection handoff, not accuracy or trusted-corpus evidence.
- Initial 10 September inspection of the actual Docker Desktop engine found one real checksum-matched four-page source, 24 candidate versions and 33 review events, with no current verified pages, ground truth, benchmarks, admitted curricula, chunks or embeddings. Historical 587-document rollout counts and its benchmark do not describe this database. The only subject is a proven migration bootstrap placeholder; no current fixture pollution was proved and no data was deleted.
- The new read-only inventory covered all 711 Grade 3/4/5 files, 696 PDF paths, 587 distinct PDFs and 5,234 unique pages, preserving before/after hashes and filesystem identity. Native text availability is not correctness: 4,090 pages have legacy Sinhala font risk and 327 have legacy Tamil font risk. All unique pages were inspected, but no OCR or human adjudication occurred in this audit. The 371-page 169,816,530-byte guide has since completed the preserved upload and bounded navigation described above. The 461-page 191,788,974-byte guide remains an untested large-document acceptance candidate in this Studio.
- The running web's security version drift was corrected from Next 16.3.1 to 16.3.4 using the committed release, without changing managed source hashes or database counts. The original laptop review audit measured only 32 pixels of usable text height at 1280×720. Candidate-only metadata correction is now deployed and demonstrated on the real multiplication source, with original intake preserved and metadata still unverified. It does not approve curriculum or source text. The real 169,816,530-byte / 371-page guide subsequently resumed its preserved session, finalized with matching source identity and completed page processing across a graceful worker restart. Its quality outcome remains blocked: 366 failed page states and five needing review, no verified pages. Last-page inspection exposed a separate unknown-language presentation fallback; source-language hint tests now cover that boundary without changing OCR/validation evidence. Renewed live navigation and the reference-only handoff are recorded above; visual/Maths fidelity and broader representative source acceptance remain separate work.
- GitHub is secondary to local product/source acceptance. The supplied runtime-job log proves npm installation succeeded with zero vulnerabilities, then Playwright's OS-dependency setup failed because Google's Chrome APT index had a hash mismatch. This is an external repository failure, not an application test result; integrity checks remain enabled. Current-change and genuine security/data-integrity defects still require fixes.

### Source fidelity and Sinhala OCR quality

- Independent evaluation-reference collection is implemented for fixed selected pages, including blocked readings, without requiring or granting source confirmation. It has separate append-only previews/revisions, original-image and actor binding, conflict recovery and evaluation-only progress counts. These references do not clear Maths/table/layout risks or enter legacy `source_page_ground_truth`; actual human reference collection and a measured multilingual comparison are still required. This workflow alone does not supply reference truth, calculate CER/WER or close the local corpus-readiness gate.
- File-backed native/OCR reading, restart-safe eight-page `SourceReadJob` batches, immutable page candidates, durable comparison images and explicit human correction/confirmation are implemented. The current worker image provides Tesseract `5.3.0` with verified `eng`, `sin` and `tam`; Tamil traineddata is installed as `tesseract-ocr-tam=1:4.1.0-2`. Language availability is prerequisite evidence, not OCR accuracy.
- The fixed coverage benchmark uses 25 originals and 40 pages (nine Grade 3, nine Grade 4, 22 Grade 5). Installed fast `sin+eng` and best Sinhala with the **same fast English** each completed 40/40 attempts; 31 text outputs differ and nine are identical. There are **zero human-adjudicated references and no CER/WER or live accuracy claim**. Completion or model disagreement does not select a quality winner or establish corpus-wide coverage.
- The controlled model comparison uses 60 seconds per command. This is not the source-reader's current 30-second default or the legacy Compose five-second profile. Preserve the earlier forced-reader profile's `tam`-missing results (nine of 40 pages) as historical prerequisite failures; the new image's Tamil installation does not retroactively change that run.
- Keep the fixed baseline and all raw UTF-8 outputs, images, hashes, engine/model/configuration versions and failure evidence immutable. Source candidates retain a separate NFC view, never a destructive NFKC rewrite. Ground truth requires an explicit human comparison of the exact candidate/version with its original page; a legacy reviewed flag, structural diagnostic, OCR result or LLM opinion is not ground truth.
- Earlier evidence from the three-page Grade 5 Parisaraya scan (`e694977...d325`) remains relevant: OCR produced 30 blocks/2,010 characters, but missed decorative cover text and matched only two of four visually checked page-2 headings. Alternate PSMs worsened content-page output; PSM 3 remains the default, not a quality guarantee. A representative legally usable, human-adjudicated Sinhala Grade 5 set is still required.
- The optional Sinhala LightOnOCR six-page feasibility attempt is **FAILED_PREREQUISITE, zero inference**: the approximately 2 GB base-weight download was incomplete and required distribution metadata was unavailable. It is not a six-page model-accuracy failure. Open-SL conversion remains disabled with licensing unresolved; DeepSeek CPU/CUDA suitability and Surya weight/output licensing have not cleared adoption. No automatic legacy-font mapping or replacement-text generation is enabled.
- Source-fidelity v2/rules-2 blocks short suspicious Latin carriers without a known-font or long-prose prerequisite, while preserving the tested legitimate mixed-language terms, acronyms, units and mathematical notation. Migration 0040 makes earlier policy results ineligible for new use without rewriting historical evidence. The heuristic still conservatively flags cases such as `getRGBx` or compact `mm;s`; it is neither a universal language classifier nor proof of a font-specific mapping. Source-fidelity v2 also blocks legacy-font native text, stale validation and unresolved Maths/layout evidence across API/UI/database gates. A bounded private four-page multiplication-PDF run selected Sinhala-only PSM 3 on every page without a language hint: the heading page was diagnostically confirmable but unverified; pages 2–4 remained failed because numbers, operators and raster/table relationships were not faithfully recovered. Eight OCR commands completed under the persisted 30-second / 300-DPI profile. Readable Unicode, a successful command and `can_confirm` are not proof of a repaired transcription; no human ground truth or CER/WER was created. Manual text edits cannot bypass unresolved source/table evidence.
- The subsequent native-equation recovery evaluation restored all three non-legacy Calibri equations on page 2 into a new bilingual-derived candidate, preserving all 16 available native anchors with zero lost/changed native tokens. Qualified selection prefers this recovery without granting confirmation; other numeric/raster evidence remains unresolved. The Sinhala-only stream's ambiguous word geometry is still refused. Pages 1, 3 and 4 retain their previous selection; raster grids/table cells and legacy Sinhala prose are not repaired by this feature. Original OCR/native candidates and the whole mathematical reference remain intact. This is a bounded source-preservation improvement, not complete transcription accuracy or arithmetic validation.
- A separate, explicitly **UNVERIFIED** offline trial used Pandukabhaya's pinned FM-Abhaya mapping (`736ae6ad85796640942b9264c9fe301dfded8a1f`) on 75 FMBindumathi runs. Native paragraphs were more legible, every native digit sequence survived and 67 Arial/Calibri runs remained byte-identical. Raster content, exact-font glyph/cluster correspondence and the legacy table header remain unadjudicated; the trial never changed application candidates or trust. Full MIT notices are available for that project, but proprietary-font/upstream UCSC provenance is not cleared, and an upstream `FMBindumathix` compatibility sample is not validation of the exact embedded `FMBindumathi` font. No mapping dependency or production converter was enabled. Open-SL declares MIT but lacks a verified full notice/upstream clearance in the inspected tree; its existing adoption block remains.
- Source image/text semantic diagnostics have an optional `create_source_semantic_diagnostics` factory, disabled by default and **not wired into general corpus reading**. The bounded adapter can only return `PASS_CANDIDATE` or `FLAG_FOR_REVIEW`, never transcription, correction, trust or publication; structural failures cannot be cleared by model agreement. No live source-semantic accuracy claim is made. This is separate from the generated-question semantic verifier below.
- Noto Sans Sinhala/Tamil are self-hosted from Fontsource `5.3.0`, with licenses in `apps/web/public/licenses/`. Package identity is not verification of an official Noto `v3.000` binary; display fonts cannot repair corrupt source text.

### Grade 3–5 pilot corpus and Scholarship policy

- The 2026-09-06 final corpus proof preserves all 711 original/evidence files and 696 PDF paths: 587 unique PDFs contain 5,234 unique-PDF pages. SHA-256, size, nanosecond mtime/ctime, inode, device and mode are unchanged, with no added/removed/changed files or errors (`.exam-guru-evidence/studio-rollout/final-original-corpus-proof/original-integrity.json`). At that historical separate-runtime checkpoint, all 587 real sources remained active but metadata-required, untrusted and unindexed. Raw inventory/footer totals are not educational authority; earlier reviewed Grade 4/5 samples belong to a separate pilot runtime.
- In the earlier corpus-rollout checkpoint, the Studio was migrated forward from `0032` to `0038_upload_request_identity`, preserving 659 source documents, 3,372 legacy pages, 57,391 blocks, 44 chunks, 51 historical questions, 29 embeddings and eight published versions. These are preservation counts, not currently eligible real-corpus content. Seventy-two exactly proven E2E sources were **QUARANTINED through the audited API**, leaving 587 normal Materials entries without deleting source rows/history. Forty-two fixture originals were already absent, proven from exact constructors/upload audits; they were not deleted or recreated by remediation. One real legacy reviewed page (`90891ee3…`, page 1) is preserved as an **unconfirmed human candidate**, not ground truth; 20 fixture legacy drafts remain unchanged.
- **At the 2026-09-06 corpus checkpoint, all 587 latest whole-document source-read jobs were completed and all 5,234 page-review states were `needs_review`**, with zero failed, verified or excluded pages and zero ground truth. These historical rollout counts are not the inventory of the separate current Docker Desktop database or evidence of v2 eligibility. There are 10,362 immutable candidate versions and 10,546 page-review events. The 116 image-failure whole-document jobs were retried through the API after separating recoverable MuPDF diagnostics from renderer stdout and handling four reader threads contending for two render slots, without enlarging budgets. Old failed job attempts/candidates/events remain history; “latest jobs completed” does not mean every historical job succeeded or any page is educationally approved.
- Preserve the 2026-09-05 legacy extraction snapshot as history, not current source-read totals: 574 originals had reviewable extraction, 13 failed `unsafe_text`, 64 retained OCR/empty-page warnings and 251 carried conservative font/glyph warnings. Categories overlap and do not establish educational quality.
- Metadata candidates remain display-only; three originals have conflicting Grade 4/5 evidence and appear as unassigned. Metadata confirmation now requires current evidence-backed admission of the active catalogue scope. A missing source year is adopted only on explicit confirmation without overwriting an existing year. **Catalogue/metadata approval is separate from page confirmation/ground truth**; each requires its own evidence. Page correction, exclusion and reread workflows exist, but human adjudication is still outstanding.
- The intake API rejects non-printable Unicode and padded labels, including non-breaking spaces. The independent PostgreSQL intake guard bounds JSON, types, lengths, ordinary whitespace and control characters, but does not reproduce every Python Unicode classification for privileged direct SQL writes. PostgreSQL's current locale treats non-breaking space as printable and not whitespace, so a generic POSIX-space/printable regex substitution is not a complete fix. Direct SQL is not a supported intake path; no trust/audit guard is relaxed.
- Earlier Grade 3 Sinhala Maths review exposed legacy-font corruption despite nonempty Unicode. The seven-page source (`a5678c4...c6058`) previously produced 4,917 native characters with `needs_ocr=false` and no OCR pages through the legacy pipeline. The new page reader routes suspicious font/script evidence even when native text is nonempty, but its candidates still require original-page comparison. The real original remains preserved and visible, not quarantined as a fixture or automatically trusted.
- Real Scholarship generation remains fail-closed: reviewed Paper I ability/reasoning/past-paper authority is missing, and Paper II lacks verified, metadata-admitted Grade 3–5 coverage. Deterministic Paper I/Paper II/full-package fixtures prove orchestration and isolation only. Term tests likewise require a reviewed term-coverage policy; filenames and generated text cannot supply it.

### Representative knowledge and retrieval quality

- Structured questions, curriculum chunks, review, versioned embeddings, hybrid retrieval and leakage-safe scope filtering are implemented. All new knowledge/history, embeddings, retrieval contexts, generation, validation, candidate creation/approval and publication require current verified page candidates, exact nonblank NFC source spans, confirmed metadata/current catalogue admission and applicable active reviewed taxonomy/scope. These checks apply at service and PostgreSQL boundaries, not just in the UI.
- Stale, edited, excluded, removed or unadmitted sources cannot authorize new downstream use. Existing legacy chunks/embeddings and published versions are preserved as history, not silently rebound or grandfathered into current eligibility. Rejection remains a safe non-promoting action.
- Current deterministic fixtures prove mechanics and isolation, not production relevance on a representative human-reviewed Grade 5 corpus.
- A bounded OpenAI `text-embedding-3-small` adapter is implemented and the final synthetic English/Sinhala live contract call passed at 1,536 dimensions, 67 input tokens, 2 microUSD and 2,142 ms. This proves transport, shape and accounting only; it is not a retrieval-quality result.
- The adapter conservatively limits each input to 8,192 UTF-8 bytes so it cannot exceed the model's documented 8,192-token input bound without introducing a runtime tokenizer dependency. Larger reviewed records fail explicitly and require meaningful educational re-chunking; they are never silently truncated.
- Successful paid calls emit content-free per-call token, integer-microUSD and latency telemetry. These values are not yet persisted as cumulative embedding-job accounting in the Operations database aggregate; provider billing remains the durable external cost source.
- P3 remains open for representative data-quality evidence.
- P4 remains open until a documented threshold is met on the agreed human-reviewed embedded fixture set.

### Historical forecasting evidence

- Deterministic statistics, rolling held-out backtests, baseline comparison and safe fallback wording are implemented.
- Current multi-year fixtures prove the algorithm and persistence mechanics rather than real Grade 5 forecasting value.
- P5 remains open until the same report is run on representative human-reviewed historical records.
- The product must use syllabus-balanced practice wording whenever measured improvement is not meaningful. It must never claim exact future-paper prediction.

### Live generation and validation baseline scope

- Opt-in OpenAI generation-to-validation baselines have been executed for the versioned English `gpt-4o-mini-2024-07-18` configuration and the currently configured `gpt-5.6-luna` Studio model, with tokens, latency, integer-microusd cost, fingerprints and validation finding codes recorded in the phase tracker.
- `gpt-5.6-luna` rejects temperature `0.0`; generation temperature is therefore explicit server-owned versioned input and is configured as `1.0` for this model rather than silently inferred from its name.
- Normal CI intentionally does not call a paid model.
- These non-failing English structured-contract runs close P8's baseline-execution gate; they are not a statistical quality study or evidence of Sinhala fluency, factual correctness, age appropriateness or semantic uniqueness.
- Every generated result remains untrusted, requires canonical validation and cannot publish without human approval.

### Multi-grade and subject-quality evidence

- The reusable model, hard retrieval filters and teacher workflow now support Grades 1–13 with subject, unit and lesson scope; Grade 7 Maths fixtures prove architecture and isolation only.
- Real educational-quality acceptance remains Grade 5-first. No Grade 1–4 or Grade 6–13 curriculum-quality claim is made from deterministic fixtures.
- The Maths checker deliberately supports only bounded exact arithmetic, fractions, decimals and percentages. Symbolic algebra, word problems and unit conversion return an explicit warning for human review rather than a false pass.
- Factual/language subjects now have an optional strict OpenAI semantic-verifier adapter. Versioned deterministic decomposition separates each selected/accepted answer, sentence-bounded explanation unit and marking criterion, then verifies the bounded claim set in one provider call with exact claim identity/order, aggregate-status and source/page-reference checks. Immutable findings retain claim outcomes, provider/model/prompt/pricing lineage and exact token/cost/latency accounting; admin Operations aggregates those records without exposing source text. This is a verification aid, not proof by model agreement.
- `deterministic-factual-claims.v1` uses stable punctuation/newline boundaries rather than a linguistic proposition parser. Abbreviations, unusual punctuation and compound sentences can over- or under-segment an explanation; unsupported or unavailable checks remain WARN and every claim remains subject to human review.
- The opt-in `gpt-5.6-terra` semantic-verifier baseline is now configured and executed: all three small synthetic reviewed English Science cases matched the expected supported, contradicted and insufficient-evidence outcomes with complete claim identity/evidence accounting. The fixed model-compatible temperature `1.0` is lineage-bound by semantic verifier version `2.1.0`. This 3/3 contract result is not a statistical quality study and cannot establish Sinhala fluency or production factual quality; missing, failed or insufficient verification remains WARN and requires human judgement.
- Reviewer revisions are audited and stale validation cannot approve edited content. Structured correction feedback, explicit promotion to immutable draft quality examples, second-reviewer approval, bounded export and deterministic replay are implemented. Promotion never trains a model or changes prompts, validators or thresholds automatically.

### Live production identity provider

- The backend supports strict OIDC JWT/JWKS validation with fixed asymmetric algorithms, issuer/audience/time checks, bounded role mapping and stable UUID subject derivation.
- The web supports authorization code flow with PKCE, single-use state, bounded token exchange, authoritative backend session validation, secure cookies and optional RP-initiated logout.
- No external provider tenant, client registration, role/group mapping, production redirect URI or credentials are available in this repository/environment.
- Route-level tests and deterministic local browser acceptance do not count as a live production IdP acceptance run.
- The current web flow stores no refresh token or ID token. Sessions end at the earlier of access-token expiry and the configured application maximum.
- Back-channel logout and provider-specific session revocation are not implemented. Local logout always clears the application session; optional RP logout depends on provider support.
- New JWKS keys become usable after the bounded JWKS cache expires. Unknown key IDs do not force an immediate network refresh, limiting attacker-controlled JWKS fetch amplification.

## Operational deployment limitations

### Observability deployment

- Content-free structured operational events, manual OpenTelemetry spans, Sentry-compatible reporting and the admin Operations dashboard are implemented.
- A production collector, dashboards, alert rules and on-call routing are deployment responsibilities and are not provisioned by this repository.
- Production ingress and access logs must redact OIDC callback query strings because authorization codes and state appear in the callback URL.

### Object-storage lifecycle

- The private Studio defaults to durable local POSIX filesystem storage with hard links, atomic replacement, directory `fsync` and Unix permissions. Resumable uploads, seekable PDF streaming and durable page-image artifacts currently require this backend; S3/MinIO support for these paths is explicitly unavailable, with no whole-file fallback.
- The legacy byte API remains 25 MiB by backend default/256 MiB maximum (Compose: 256 MiB), with a 26 MiB legacy browser proxy limit. Materials uses separate 4 MiB resumable parts, owner-scoped request identity and receipt-prefix checks. Default 8 GiB/owner, 32 GiB/global and eight-active-session/owner reservations are **operational quotas**, not universal PDF limits. Retained completed/failed staging still counts; no automatic purge or quota release on browser-checkpoint removal exists.
- The corpus importer now uses the supported 4 MiB resumable API and existing reader outbox, with owner/origin/runtime/source-bound ledgers, complete receipt-prefix verification and bounded mutation recovery. A missing saved session pauses rather than silently recreating it; legacy-ledger migration is explicit and preserves history. The runtime has no unique installation UUID, so saved-session and exact request/source/receipt checks remain essential. Synthetic large-file/recovery tests are not real-corpus upload proof. The browser/importer can pause; definitive server cancellation/expiry remains unimplemented.
- Source-read jobs resume in eight-page batches without the legacy 40/1,000-page bounds; per-command, batch, text, raster and filesystem resource limits remain. Durable `STORAGE_ROOT/fidelity-page-images` references bind source/page/version/hash/size and are checked on read. A missing declared artifact fails closed, so recovery needs the artifact backup or an explicit reread, not silent rendering as a cache fallback. `SOURCE_READ` protects image/PDF access, including bounded original-PDF Range streaming.
- Existing MinIO/S3 source objects are not migrated automatically. Switching an existing database to local storage requires explicit checksum-verified export/import; retained MinIO volumes must not be deleted before migration and backup verification.
- Local reconciliation tags are bounded atomic sidecars owned by the application; they do not modify immutable source bytes and are not interchangeable with arbitrary S3 operator tags.
- Scheduled reconciliation detects unreferenced content-addressed source objects after a grace period and persists candidate/resolution evidence.
- Reconciliation never deletes objects. Optional app-owned candidate tags are disabled by default and require explicit production configuration.
- Any lifecycle deletion rule requires separate operator approval, versioning/retention controls and recovery validation. It must never target the live `sources/` namespace without the documented candidate-tag conditions.

### Backup and recovery policy

- Guarded PostgreSQL backup/restore scripts and the [local Studio recovery runbook](../ops/BACKUP_RESTORE.md) cover the current release's coordinated database/artifact contract. The actual restore integration passed on synthetic data; it is not a persistent-Studio restore. Scripts cover only PostgreSQL; separately copy and verify original sources, sidecars, retained upload chunks and page images. Resolve the selected release's migration head, restore into a new empty isolated database/filesystem with correct private ownership/modes, and validate exact artifact/lineage state before any recovery actor runs.
- Private on-host pre-remediation bundles at `.exam-guru-evidence/backups/pre-fidelity-20260906T132054Z` and `.exam-guru-evidence/backups/pre-fidelity-20260906T151115Z` passed verification-only restore checks. They are **not off-host backups and not proof of a production restore**. No destructive restore has been run against a persistent project database.
- Include immutable originals, retained `.source-uploads`, `fidelity-page-images`, storage sidecars and matching PostgreSQL state in a coordinated recovery inventory. Production RPO, RTO, retention, encryption/KMS, off-site replication and full restore-drill frequency remain deployment decisions. Use protected temporary `PGPASSFILE` credentials via the approved local helper; never discover or log secrets.

### Abuse and network controls

- Authenticated costly operations have atomic per-principal Valkey limits and fail closed when the limiter is unavailable.
- These controls are not edge DDoS protection. Production ingress must independently enforce unauthenticated, per-IP and network-level limits.

### Production configuration

- Production settings reject deterministic identity, deterministic generation/embeddings, local credentials, insecure service connections and disabled application cost controls.
- A deployment still needs real TLS endpoints, database/Valkey credentials, a private writable local storage root (or optional S3 credentials), OIDC configuration, OpenTelemetry/Sentry destinations and explicit credentials/versioning/pricing for every enabled live AI provider.

## Product boundaries

- Retrieved/uploaded text is untrusted data and can contain prompt-injection-like content. It never authorizes an action.
- Lexical n-gram duplicate detection is not semantic paraphrase detection and can produce false positives or negatives.
- Automated validation cannot approve or publish content. Human review remains mandatory.
- Published paper serving requires no live model call, but Priority 2 student product work remains blocked until P10 is legitimately complete.

## Verification checkpoint and gate impact

After the Generate Papers catalogue correction, the full local backend gate passed 4,887 tests with four expected optional skips, one separately executed backup-test deselection and 100% statement/branch coverage. Frontend passed 780 tests with two workers and all configured coverage metrics at 100%; existing admin-component coverage exclusions remain unchanged, with separate component/E2E evidence. The final full isolated E2E suite passed 25/25 without skips. Lint, type checks, production builds, dependency audit, required backup static checks and the separate restore integration passed. Interactive DOM, image and viewport checks passed; the optional MCP screenshot capture timed out. Three known non-gating warnings remain recorded separately. These local checks establish neither remote CI success nor educational quality.

Generate Papers now filters by current admission at the backend, binds selectors to grade/medium/curriculum, and rejects stale source-scope fingerprints at preview/creation/preparation boundaries. Live verification confirms the empty approved-catalogue message without fixture labels; advanced configuration and published history remain intact. All latest corpus reading jobs have completed, but human adjudication/metadata approval, representative knowledge/retrieval/backtesting evidence and live external OIDC acceptance are still missing. P2/P3/P4/P5 and P10 are not promoted; Priority 2 remains blocked. [PHASE_TRACKER.md](PHASE_TRACKER.md) owns the exact final post-fix counts, warnings, runtime evidence and release/CI outcome; earlier passing runs cannot replace that verification.
